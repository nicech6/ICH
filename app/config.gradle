ext {
    // 用于编译的SDK版本
    COMPILE_SDK_VERSION = 28
    // 用于Gradle编译项目的工具版本
    BUILD_TOOLS_VERSION = "28.0.2"
    // 最低支持Android版本
    MIN_SDK_VERSION = 17
    // 目标版本
    TARGET_SDK_VERSION = 28
    //版本号
    VERSION_CODE = 15//要发的版本
    // 设置是否使用混淆
    MINIFY_ENABLED = true
    MINIFY_DISABLED = false

    AROUTE_REGISTER="1.0.2"
    AROUTE_API="1.5.0"
    AROUTE_COMPILER="1.2.2"

}